[tool.ruff]
# Longitud de línea
line-length = 88
target-version = "py310"

fix = true

[tool.ruff.lint]
# Habilitamos validaciones de errores (E, F) y el ordenamiento de imports (I)
select = ["E", "F", "I", "UP", "B"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"